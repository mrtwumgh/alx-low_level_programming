#include <stdio.h>

/**
*main - entry point to the code
*Description - prints the first 50 fibonacci numbers
*Return: a value of 0
*
*/

int main(void)
{
	long int a;
	long int b;

	a = 1;
	B = 2;
	for (c = 1; c <= 50; ++c)
	{
		if (a != 20365011074)
		{
			printf("%ld, ", a);
		} else
		{
			printf("%ld\n", a);
		}
		next = a + b;
		a = b;
		b = next;
	}
	return (0);
}
