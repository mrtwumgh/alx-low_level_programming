#include <stdio.h>

/**
 * main - A C program that prints using printf
 * Return: Program should return 0
 * Program should compile without warnings when using the -wall gcc option
 *
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
