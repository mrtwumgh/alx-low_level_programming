#include <stdio.h>

/**
 * main - A C program that prints a string using the Puts Function
 * Displays the string "Programming is like building a multilingual puzzle
 * Return: Program should end with the value 0
 *
 */

int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
