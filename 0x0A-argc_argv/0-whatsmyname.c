#include <stdio.h>

/**
  * main - Entry point to the code
  *@argc: counter for arguments
  *@argv: vector of strings with arguments to be passed
  *Return: 0
  *
  */

int main(int argc, char *argv[1])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
