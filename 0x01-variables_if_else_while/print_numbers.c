#include <stdio.h>

/**
*main - Entry point for code
*Description - prints out all numbers in base 10
*Return: should return a value of 0
*
*/

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
